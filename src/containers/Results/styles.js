import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  footer: {
    margin: 0,
    position: "absolute",
    top: "90%",
    left: "50%",
    transform: "translate(-50%, 90%)",
    textDecoration: "none",
  },
}));
